function printData() {
  push();
  textSize(50);
  fill(255, 0, 0);
  let texto;
  textoamplitud = "amplitud" + amplitud; //    esto lo hice para ver en pantalla la amplitud
  text(textoamplitud, 50, 50);
  pop();
}
